# 事故をほぼゼロにする最小ルール

## ルール1：作業開始の儀式はこれだけ

作業を始める前に **必ず1回だけ** 実行します。

```bash
git status
```

**目的：** いま _どのブランチ_ で、汚れてるか（未コミットがあるか）を必ず目視する

---

## ルール2：コミットしたら必ず「push までが1セット」

コミットした直後に **必ず** これを打つ。

```bash
git push
```

**目的：** ローカルだけ進んでる事故（今回のやつ）を消す

---

## ルール3：PCをまたぐ前に「差分ゼロ確認」

家→会社 / 会社→家 に移る直前に必ずこれ。

```bash
git log --oneline --decorate -5
```

**目的：** HEADのコミットIDが見える

これが一致してれば「同じ状態」だと判断できる

⑥ 会社PCで作業するときの儀式（復習）

毎回これだけでOKです。

作業開始：

git pull
git status

作業終了：

git add .
git commit -m "..."
git push

PC移動前：

git log --oneline --decorate -5