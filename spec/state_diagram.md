# MTG Deck Gate
# State Diagram

```mermaid
stateDiagram-v2
    [*] --> 起動ゲート

    state 起動ゲート {
        [*] --> 起動メニュー
        起動メニュー --> カテゴリゲート: 1 新規デッキ作成
        起動メニュー --> カテゴリゲート: 2 引き継ぎデータ読み込み
        起動メニュー --> カテゴリゲート: 3 COLLECTION編集
    }

    state カテゴリゲート {
        [*] --> TAG説明表示
        TAG説明表示 --> カード選択: c
    }

    state カード選択 {
        [*] --> 候補表示
        候補表示 --> 候補表示: 1 / 2 / 3 / 4 採用
        候補表示 --> 候補表示: o
        候補表示 --> 候補表示: d
        候補表示 --> 候補表示: ct*
        候補表示 --> 候補表示: cta
        候補表示 --> 候補表示: ls
        候補表示 --> aic: aic
        候補表示 --> カテゴリ完了: end
        候補表示 --> 停止: s
        候補表示 --> Export処理: ex
    }

    state aic {
        [*] --> AI候補表示
        AI候補表示 --> AI候補表示: aio
        AI候補表示 --> AI候補表示: air*
        AI候補表示 --> AI候補表示: ct*
        AI候補表示 --> AI候補表示: cta
        AI候補表示 --> カード選択: aip
        AI候補表示 --> カード選択: aicc
    }

    state カテゴリ完了 {
        [*] --> 完了表示
        完了表示 --> カテゴリゲート: c
        完了表示 --> デッキ編集: 全カテゴリ終了
    }

    state デッキ編集 {
        [*] --> 編集画面
        編集画面 --> 編集画面: r*
        編集画面 --> 編集画面: ai / as / am / af / ap
        編集画面 --> 編集画面: ri / rs / rm / rf / rp
        編集画面 --> 編集画面: autoland
        編集画面 --> 編集画面: ct*
        編集画面 --> 編集画面: cta
        編集画面 --> Export処理: ex
    }

    state Export処理 {
        [*] --> チェック実行
        チェック実行 --> 排出確認: CI/レアリティ警告あり
        チェック実行 --> Export排出: 問題なし
        排出確認 --> Export排出: y
        排出確認 --> カード選択: n（カード選択から来た場合）
        排出確認 --> デッキ編集: n（デッキ編集から来た場合）
        Export排出 --> カード選択: カード選択から来た場合
        Export排出 --> デッキ編集: デッキ編集から来た場合
    }

    停止 --> [*]